package demo24;

public class Triangle {
  /**
   * ?§Ø??????¦Å????????? a, b, c ?????????¦Å??????
   * <p>
   * ?????????
   * ? 0 ????????????¦²?
   * ? 1?????????????¦²?
   * ? 2 ??????????????¦²?
   * ? 3 ?????????????¦±?
   */
  public static int comfirmTriangle(int a, int b, int c) {
    if (a <= 0 || b <= 0 || c <= 0) {
      throw new IllegalArgumentException("sorry, input of UnQualified");
    }
    if ((a == b && b == c) && ordinary(a, b, c)) {
      return 3;
    }
//    Isosceles
    if (isosceles(a, b, c) && ordinary(a, b, c)) {
      return 2;
    }
    if (ordinary(a, b, c)) {
      return 1;
    }

    return 0;
  }

  private static boolean isosceles(int a, int b, int c) {
    return (a == b && b != c) || (b == c && a != b) || (a == c && b != c);
  }

  private static boolean ordinary(int a, int b, int c) {
    return a + b > c && a + c > b && b + c > a && a - b < c && a - c < b && b - c < a;
  }

}
